spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL57InnoDBDialect"
  datasource:
    url: "jdbc:mysql://localhost:3306/itemsharing"
    username: "root"
    password: "root"
    driverClassName: "com.mysql.jdbc.Driver"

security:
  basic:
    enabled: false

server:
  port: 8081
